/*abc248d*/
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int n, q;
    cin >> n;
    vector<int> a(n);
    vector<int> numx[n];
    // numx[i][]で数字iがどの場所にあるか。
    rep(i,n) {
        cin >> a[i];
        numx[a[i]];
    }
    
    return 0;
}
